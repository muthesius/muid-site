{{#if showSlides}}
  {{#slide-show slides=model.images as |slide|}}
    <img src={{attachment-url model.id slide "thumb-large"}}>
  {{/slide-show}}
{{/if}}
<section class="info">
	<h1>{{model.title}}</h1>
	<h2>{{model.authors}}</h2>
  {{#if model.abstract}}
	  <section class="abstract">{{model.abstract}}</section>
  {{/if}}
	{{#if model.description}}
		<section class="description">{{model.description}}</section>
	{{/if}}
</section>
