<header>
	<nav>
		<a href="#">About MUiD</a>
		<a href="#">News °</a>
		<a href="#">Apply</a>
	</nav>
	{{muid-logo "index"}}
	{{search-bar placeholder="looking for something else" buttonText="?"}}
</header>

<section role="main">
	{{!-- Streams --}}
	{{#each facets as |f|}}
		{{#project-stream topic=f as |project|}}
			{{project-card content=project}}
		{{else}}
			Nothing found
		{{/project-stream}}
	{{/each}}
	{{!-- Nested Routes --}}
	{{outlet}}
</section>

<footer>
	<a href="#">imprint</a>
	<img src="/images/muthesius-logo.png" alt="Muthesius Academy of Fine Arts & Design">
</footer>
