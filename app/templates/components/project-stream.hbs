{{#if topicTitle}}
	<h3>{{topicTitle}} <small>{{#if search.loading}}(searching){{/if}}</small></h3>
	{{#each content as |project|}}
		{{yield project}}
	{{else}}
		<p class="none">{{yield to="inverse"}}</p>
	{{/each}}
{{/if}}