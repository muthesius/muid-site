{{#if content.isLoading}}
	Loading project {{from.ref}}
{{elseif empty}}
	Could not load Project
{{else}}
	{{#link-to 'project.detail' content.id tagName="figure"}}
		<img src="{{content.posterLink}}" alt="">
		<caption>
			{{content.title}}<br>{{content.authors.firstObject}}
		</caption>
	{{/link-to}}
{{/if}}
